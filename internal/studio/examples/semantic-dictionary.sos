+++
version = 1
[interpretation]
mode = "semantic"
[runtime]
judgment = "semantic"
[budget.run]
requests = 8
timeout = "30s"
+++
# Semantic dictionary demo — no token or provider request is needed.
# Analyze shows which phrases matched registered language definitions.

make age 21
if age bigger 18 show "adult"

make score 10
provided that score at least 10 display "qualified"
