# Jev is optional

Offline editing, running, checking, building, and debugging work immediately. Add a Jev token only for model-backed operations; VS Code stores it in encrypted SecretStorage rather than workspace files.
