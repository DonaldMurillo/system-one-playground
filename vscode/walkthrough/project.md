# Project controls, not another file browser

The SysOneScript panel keeps project actions together while Explorer remains the place for files. The bundled runtime means Run, Check, Build, and Debug work without installing Go or SysOneScript separately.
