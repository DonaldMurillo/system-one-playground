# Output you can actually see

Run, Check, and Build automatically reveal the **SysOneScript Run** output channel. The panel keeps a concise status, while the output channel retains the command, program output, errors, and exit result.
