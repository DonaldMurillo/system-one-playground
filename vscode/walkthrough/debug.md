# A real debugger

Use ordinary VS Code breakpoints and controls. SysOneScript supports stepping, stack frames, locals, conditional breakpoints, logpoints, and read-only expression evaluation through its native debug adapter.
